doctype html
html(lang="uk")
  head
    meta(charset="UTF-8")
    title Football News
    style.
      body.light {
        background-color: #fff;
        color: #000;
        font-family: Arial, sans-serif;
      }
      body.dark {
        background-color: #121212;
        color: #fff;
        font-family: Arial, sans-serif;
      }
      a, button {
        color: inherit;
        text-decoration: none;
        background: none;
        border: 1px solid currentColor;
        padding: 8px 16px;
        margin: 4px;
        border-radius: 6px;
        cursor: pointer;
      }
      a:hover, button:hover {
        opacity: 0.7;
      }
  body(class=theme)
    header
      h1 Football News
      p Поточна тема: #{theme}

      // --- Перемикач теми
      form(action="/set-theme" method="POST" style="display:inline;")
        if theme === "light"
          input(type="hidden" name="theme" value="dark")
          button(type="submit") 🌙 Увімкнути темну тему
        else
          input(type="hidden" name="theme" value="light")
          button(type="submit") ☀️ Увімкнути світлу тему

      // --- Авторизація
      if user
        p Привіт, #{user.username}!
        a(href="/dashboard") Перейти в кабінет
        a(href="/logout") Вийти
      else
        a(href="/login") Увійти
        a(href="/register") Реєстрація

    main
      h2 Ласкаво просимо на сайт новин про футбол!
      p Це головна сторінка. Тут можна перемикати тему і авторизуватися.
